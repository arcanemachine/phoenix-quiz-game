<!DOCTYPE html>
<html lang="en" class="h-full">
  <head>
    <%!-- meta --%>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <meta
      name="description"
      content={
        assigns[:tag_meta_description] ||
          "A fun and educational quiz game. Practice alone, or compete against your peers!"
      }
    />

    <%!-- title --%>
    <.live_title suffix=" - Quiz Game">
      <%= assigns[:tag_title] || assigns[:page_title] || "Quiz Game" %>
    </.live_title>

    <%!-- css --%>
    <link phx-track-static rel="stylesheet" href={~p"/assets/app.css"} />

    <%!-- js --%>
    <script defer phx-track-static type="text/javascript" src={~p"/assets/app.js"} />

    <script>
      // if saved theme exists, then set theme before initial render
      if (localStorage.getItem("theme"))
        document.documentElement.dataset.theme = localStorage.getItem("theme");
    </script>
  </head>
  <body class="flex flex-col h-full bg-base-100 antialiased">
    <ul class="relative z-10 flex items-center gap-4 px-4 sm:px-6 lg:px-8 justify-end">
      <%= if @current_user do %>
        <li class="text-[0.8125rem] leading-6 text-zinc-900">
          <%= @current_user.email %>
        </li>
        <li>
          <.link
            href={~p"/users/settings"}
            class="text-[0.8125rem] leading-6 text-zinc-900 font-semibold hover:text-zinc-700"
          >
            Settings
          </.link>
        </li>
        <li>
          <.link
            href={~p"/users/log_out"}
            method="delete"
            class="text-[0.8125rem] leading-6 text-zinc-900 font-semibold hover:text-zinc-700"
          >
            Log out
          </.link>
        </li>
      <% else %>
        <li>
          <.link
            href={~p"/users/register"}
            class="text-[0.8125rem] leading-6 text-zinc-900 font-semibold hover:text-zinc-700"
          >
            Register
          </.link>
        </li>
        <li>
          <.link
            href={~p"/users/log_in"}
            class="text-[0.8125rem] leading-6 text-zinc-900 font-semibold hover:text-zinc-700"
          >
            Log in
          </.link>
        </li>
      <% end %>
    </ul>
    <%!-- utility --%>
    <div>
      <div class="hidden">
        <%!-- search engine description --%>
        A fun and educational quiz game. Practice alone, or compete against your peers!
      </div>

      <noscript>
        <h1 class="py-4 text-3xl bg-gray-700 text-white text-center">
          Please enable Javascript to enable full functionality for this website.
        </h1>
      </noscript>
    </div>

    <.navbar />

    <main class="grow flex flex-col max-w-4xl mt-8 mx-auto mb-20 container px-4 sm:px-10 md:px-20">
      <section :if={assigns[:page_title] || assigns[:page_subtitle]} class="mb-10">
        <%!-- page title/subtitle --%>
        <h1 class="text-4xl text-center font-bold underline">
          <%= assigns[:page_title] %>
        </h1>
        <h2 :if={assigns[:page_subtitle]} id="page-subtitle" class="mt-2 text-2xl text-center">
          <%= assigns[:page_subtitle] %>
        </h2>
      </section>

      <%= @inner_content %>
    </main>

    <.footer />
    <.toast_container />
  </body>
</html>
